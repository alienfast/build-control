[
  {
    "WARNING!!!!!!!!!!!!!!!": "Any changes here have to be pushed to the remote in the scenarios dir because this is immediately wiped out and cloned.  git clone remote repo && cd repo.  make changes. git commit -m 'config'. now push our repo up to github (kind of meta, pushing a repo to a repo...)",
    "remote":{
      "repo": "../"
    },
    "cwd": "build",
    "branch": "build",
    "connectCommits": false,
    "commit": {
      "template": "a build commit for the untracked branch scenario"
    }
  }
]
